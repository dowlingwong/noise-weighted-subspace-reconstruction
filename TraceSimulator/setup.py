from setuptools import setup,find_packages

setup(
    name='TraceSimulator',
    version='0.0.0',
    author='DELight Collaboration',
    author_email='francesco.toschi@kit.edu',
    description='Simulation tool for traces for the DELight experiment.',
    packages=find_packages()
)
