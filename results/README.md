# Results Layout

Suggested structure:
- `models/`: trained model artifacts
- `tables/`: JSON/CSV summaries used by paper
- `figures/`: generated plots

Do not commit large binary outputs unless they are minimal release artifacts.
